import React from "react";
import { Card, CardHeader, CardActions, CardContent, Button, Input } from "@mui/material";
import { TextField } from "@mui/material";
import LoginComponent from "../components/login";

const Login = () => {
    return (
        <div>
           <LoginComponent />
        </div>
    )
}

export default Login;